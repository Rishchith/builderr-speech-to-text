# Core ASR engine — local, offline, MIT licensed
# Weights: openai/whisper (MIT) — downloaded once, cached locally
faster-whisper>=1.0.3

# Audio I/O
soundfile>=0.12.1

# Numpy (required by faster-whisper PCM path)
numpy>=1.24.0
